<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.LoginHistoryMapper">

    <!-- ========================================================= -->
    <!-- 로그인 기록 저장                                           -->
    <!-- ========================================================= -->
    <insert id="insertLoginHistory">
        INSERT INTO login_history (user_id, login_time, device_info, location)
        VALUES (#{user_id}, NOW(), #{device_info}, #{location})
    </insert>

    <!-- ========================================================= -->
    <!-- 마지막 로그인 기기 → 중복 저장 방지용                      -->
    <!-- ========================================================= -->
    <select id="getLastDevice" resultType="string">
        SELECT device_info
        FROM login_history
        WHERE user_id = #{user_id}
        ORDER BY login_time DESC
        LIMIT 1
    </select>

    <!-- ========================================================= -->
    <!-- 최근 로그인 기기 5개                                       -->
    <!-- ========================================================= -->
    <select id="getRecentDevices" resultType="string">
        SELECT device_info
        FROM login_history
        WHERE user_id = #{user_id}
        ORDER BY login_time DESC
        LIMIT 5
    </select>

    <!-- ========================================================= -->
    <!-- 바로 이전 접속 지역 (두 번째 기록)                         -->
    <!-- ========================================================= -->
    <select id="getLastLocation" resultType="string">
        SELECT location
        FROM login_history
        WHERE user_id = #{user_id}
        ORDER BY login_time DESC
        LIMIT 1 OFFSET 1
    </select>

</mapper>
