<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.UserMapper">

   <!--  회원 가입 -->
   <insert id="insertUser" parameterType="com.itwillbs.domain.UserVO">
   
      INSERT INTO Users (user_id, user_password, user_name, user_phone, user_email, user_address, user_gender, user_role, user_status, grade_id, points)
                  VALUES (#{user_id}, #{user_password}, #{user_name}, #{user_phone}, #{user_email}, #{user_address}, #{user_gender}, 'user', 'active', 1, 0)
   </insert>
   
   <!-- 아이디 중복 확인 -->
   <select id="getUserById" parameterType="String" resultType="com.itwillbs.domain.UserVO">
      SELECT * FROM Users WHERE user_id = #{value};
   </select>

</mapper>