<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.UserMapper">

<!-- <<<<<<< HEAD -->
	<!--  회원 가입 -->
	<insert id="insertUser" parameterType="com.itwillbs.domain.UserVO">
	
		INSERT INTO user (user_id, user_password, user_name, user_phone, user_email, user_address, user_gender, user_role, user_status, grade_id, points)
		            VALUES (#{user_id}, #{user_password}, #{user_name}, #{user_phone}, #{user_email}, #{user_address}, #{user_gender}, 'user', 'active', 1, 0)
		
	</insert>
	
	<select id="login" parameterType="com.itwillbs.domain.UserVO" resultType="com.itwillbs.domain.UserVO">
		
		SELECT *
		FROM user
			WHERE user_id = #{user_id}
		  AND user_password = #{user_password}
	</select>
	
	<select id="selectUserById" parameterType="String" resultType="com.itwillbs.domain.UserVO">
    	SELECT * 
    	FROM user
    	WHERE user_id = #{user_id}
	</select>

</mapper>


