<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.MemberMapper">
<!-- ?,?,? -> 대신 MemberVO 멤버변수 이름 작성 #{멤버변수이름} -->
	<update id="updateProMember">
		update Users
		set user_id = #{user_id},user_password = #{user_password},
		user_name = #{user_name},user_phone = #{user_phone},
		user_email = #{user_email}, user_file = #{user_file}
		where user_id = #{user_id}
	</update>
	
	<select id="updateMember" parameterType="string" resultType="com.itwillbs.domain.UserVO">
		select * from Users
		where user_id = #{user_id}
	</select>
	


</mapper>