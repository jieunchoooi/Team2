<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.itwillbs.mapper.LoginLogMapper">

    <insert id="insertLoginLog">
        INSERT INTO login_log (user_id, ip, device, location)
        VALUES (#{user_id}, #{ip}, #{device}, #{location});
    </insert>

    <select id="getRecentLogs" resultType="com.itwillbs.domain.LoginLogVO">
        SELECT *
        FROM login_log
        WHERE user_id = #{user_id}
        ORDER BY login_time DESC
        LIMIT 5
    </select>

</mapper>
